# ISSUE — Tracker Perbaikan & Kualitas ark-watch

> Dokumen ini = satu-satunya tempat tracking hal yang keliru / kurang / menipu.
> Cara pakai: 1 temuan = 1 ID. Status di-update saat diperbaiki (cantumkan commit).
> Sumber: audit 3-agen 2026-09-02 + workflow scan iteratif 2026-09-02..04 (gelombang 1-5).
> Setiap klaim "FIXED" wajib ada bukti (commit + test/output).

**Status:** `OPEN` · `IN-PROGRESS` · `FIXED` (selesai + bukti) ·
`PARTIAL` (sebagian; sisa dijelaskan) · `DEFERRED` (sengaja ditunda — alasan) · `WONTFIX`

**Kelas:** `BUG` (perilaku salah) · `MISLEAD` (angka/interpretasi bisa menipu) ·
`QUAL` (kualitas/kebersihan kode) · `SPEC` (deviasi dari docs/ plan) · `GATE` (bukti runtime)

---

## A. OPEN saat ini

| ID | Kelas | Temuan | Status | Catatan |
|---|---|---|---|---|
| I-022 | SPEC | UX-SPEC §4 bad-day rendering penuh (suffix ⚠karantina/basi/fallback + health.csv) | DEFERRED (D-003) | Kalibrasi pasca-soak dgn data riil |

*(I-021 FIXED 2026-09-02: schema_fp sha1-bentuk-respons + alarm drift + quota_used di harvest — live 14/14; cakupan = permukaan registry/ROUTES, harvester gray dilindungi parser-fixture tests. I-023 FIXED: catatan interpretasi basis-waktu gate FedWatch di UX-SPEC §0.)*

## B. DEFERRED — sengaja ditunda (bukan bloker soak)

| ID | Temuan | Alasan |
|---|---|---|
| D-002 | ~~FR-28 WGC manual CLI~~ **CLOSED 09-10**: tidak pernah sekali pun dipakai (0 baris wgc_gold di DB — owner konfirmasi) dan kebutuhannya sudah digantikan penuh oleh LBMA gold otomatis (122 bulan, gelombang-6) | CLI 15-baris dibiarkan (degradable, tak berbiaya); label brief mati dibersihkan saat sentuhan berikutnya |
| D-003 | = I-022 bad-day rendering | Pasca-soak |
| D-004 | ADR-010 mesin episode Blok-C (min_months/peak_exit) | Finalisasi F3/F4 + kalibrasi 2.5/3/3.5  — **ANOTASI TRIASE 09-10**: mesin episode_blok_c SUDAH TERIMPLEMENTASI penuh di qa/f4.py:341-466 (trailing-peak, peak_exit, re-entry fall-through) — yang tersisa = kalibrasi 2.5/3/3.5 dgn data + pin owner; PARTIAL bukan UNBUILT
| D-005 | Backtest point-in-time penuh: release_ts historis = 'na' (244k baris backfill pra-fitur); label NBER | Replay jujur butuh re-fetch historis dgn realtime_start per-obs — proyek F4; FORWARD sudah diisi (I-001 PARTIAL→FIXED utk data baru) |
| D-006 | Ekspektasi ECB (ESR implied path) + ESTRWatch | **RESOLVED 2026-09-10 (ESTRWatch LANDED)**: riset 5-agen (metodologi publik + spec kontrak + kalender resmi ecb.europa.eu) → build min-norm joint-solve (matriks desain rank-7-dari-11 STRUKTURAL — OLS biasa kolaps; numpy dideklarasikan eksplisit) → review adversarial 24-agen: 18 temuan valid SEMUA difix (off-by-one third-Wednesday kasus bulan-berawal-Kamis [latent s.d. 2028, 4 kontrak strip hari ini salah-jendela]; cooldown permanen menelan re-flip → kunci per tanggal snapshot; strip-stale silent → gate umur + tanggal ESR di baris brief; FK fresh-DB; fetch_first_ts 550KB → firstNObservations; exception boundary f2; ≈-marker rapat berbagi kuartal; delta-bp selalu tampil; golden anchor ECB:ESTR; sanity gate strip ±plausibilitas). Live: rms 0.68bp, fitted=aktual ≤1.5bp, “hike ≈85% (Sep-10, +21bp → DFR 2.46%, ESR 09-09)”; €STR backfill 1.778 obs (2019→). Docs reversal di explained/01 (3 titik) + methodology/estrwatch.md. Sisa v2-opsional: view QuikStrike ESTRWatch (tool skenario); xccy._imm_approx 2-hari-awal = tiket terpisah |
| D-007 | explore `--trace` + kolom Δ/z | Nice-to-have |
| D-008 | FR-39 id-series drill-down per angka brief | UX work |
| D-009 | S-2 format `(cons → implikasi)` | Butuh mapping series↔event |
| D-010 | FR-55 re-kalibrasi kuartalan otomatis | Manual via verify harian |
| D-013 | `misc.py` (FMP holidays, MPT mortgage) | Pendukung FR-16/MPT — F2-lanjut  — **ANOTASI TRIASE 09-10**: SEBAGIAN STALE — FMP:RECESSION_PROB sudah live via misc.py (fetch_log OK berulang, aktif di registry blocks_d-f); yang benar-benar belum terpakai hanya holidays + MPT mortgage → dipersempit ke dua fungsi itu
| D-014 | Celah retensi NY Fed v9 (klas CME-settlements, disadari & diterima): (a) agency SOMA mingguan tanpa jalur `--asof` — Jumat gagal = minggu hilang permanen; (b) 12 seri rates = latest-only (downtime ≥2 run = lubang permanen vs FRED self-heal); (c) ops `last/14` = 14 OPERASI bukan hari — downtime panjang kehilangan op; endpoint `search` tersedia utk catch-up | Review ronde-1 2026-09-03. Acceptable utk data pelengkap; `soma harvest --asof` = kandidat bila minggu kosong benar terjadi. Kalibrasi threshold options_*/dealer_* juga menunggu 2-8 minggu data (provenance di params_signals.yaml)  — **TRIASE 09-10**: skenario (a) nyaris terjadi — minggu as-of 09-02 belum pernah masuk (job Jumat 09-04 gagal di jendela outage) → di-RESCUE manual 09-10 14:39 (433 CUSIP + agency 8.956 + WAM; tabel kini kontinu s.d. 09-02); next as-of = 09-09 (job Jumat 09-11 normal). Jalur --asof tetap kandidat
| D-015 | Restrukturisasi docs oleh owner 2026-09-03: DATA-SPEC/BUILD-PLAN/UX-SPEC/ADR/… dihapus; finalisasi commit 296a799 (2026-09-04). Memori persisten kini = pesan commit (provenance terperinci) + komentar kode + provenance params_signals.yaml; dokumen lama retrievable dari git history | Kebijakan owner final. Referensi §dokumen lama di komentar kode = rujukan historis (ambil via git show); P5/REFACTOR-PLAN tidak lagi terjadwal |
| D-018 | Kapasitas jangka panjang (review skalabilitas 2026-09-08, angka terukur): (a) `cme_options_settlements` tumbuh 23.7k baris/hari TANPA jalur purge — satu-satunya konsumen jangka panjang adalah trailing-range PCR trigger 4f yang hanya butuh agregat harian → kandidat rollup (trade_date, product, pcr) diisi saat harvest + purge per-strike >90 hari (~0.65-0.8 GB/tahun jika tidak); (b) backup retensi 30-harian+12-bulanan dari file yang tumbuh ≈ 25-30 GB pada bulan-12 → pertimbangkan diferensial/mingguan; (c) heartbeat daemon ditulis hanya di awal loop → job >5 menit (verify terukur 221-269s) tinggal selangkah dari kontrak stale-5-menit — pindahkan _heartbeat() ke dalam _run_job wrapper; (d) `_auction_history` full-scan 11.1k baris tiap 60s (bounded, ms-level — cache percentile per auction_date bila trigger 4j mulai terasa) | **(c) FIXED 09-10** — _heartbeat() kini dipanggil juga di dalam wrapper _run_job (daemon.py), kontrak stale-5-menit aman utk job panjang. (a) RE-TRIASE 09-10: tabel 70.873 baris (~3 hari penuh; proyeksi 250k ≈ 15 Okt 2026) — rollup+purge makin mendesak, jadwalkan sebelum mid-Okt; (b) deferred s.d. backup >2-3GB; (d) deferred (bounded) |
| D-019 | Insiden daemon: mati "tanpa penanda" BERULANG — (1) 2026-09-05 01:44 → 09-08 15:59 WIB; (2) 2026-09-09 00:26 → 09-10 13:47 WIB. **AKAR MASALAH DITEMUKAN 09-10 (Windows Event Log)**: PC dimatikan/di-restart — shutdown via Start Menu 09-09 00:26:15 (log daemon berhenti persis 00:26:14) + restart Windows Update 09-10 09:08; daemon python konsol tidak mungkin selamat. Bukan bug kode; pola "mati tengah malam" = kebiasaan shutdown PC malam hari. Dampak kumulatif: backup malam hilang, job pagi tertunda, CME settlements+options berisiko lubang permanen (retensi ±5 hari): per 09-10 siang trade-date terakhir = 09-04 → 09-05 s.d. 09-09 menunggu walk-back pasca-restart 13:47 (09-05 dipublikasi ~09-08 → masih dalam retensi, tapi mepet). | **RESOLVED 09-10 (data-side)**: walk-back daemon + backfill eksplisit menyelamatkan 09-03 (393+23.409 ops), 09-08 (372+23.457 ops), 09-09 (393+23.738) — SEMUA masih dalam retensi saat dikejar. Lubang final: 09-05 PERMANEN (retensi habis, 0 baris), 09-07 = Libur Buruh (tak ada data), 09-08 ES-strip saja (endpoint balas strip 09-04 utk pid 133 — fetcher aman menyimpan di bawah tanggal respons; 21 baris futures ES hilang, minor). TERSISA keputusan owner: Task Scheduler "saat startup" |
| D-020 | Bybit konektivitas INTERMITEN level-TCP dari jaringan ini (terverifikasi 09-08: ditolak menit-menit, lalu jendelah 200 singkat, lalu ditolak lagi; job f2 produksi sukses di sebagian jendela). Ekstra positioning (audit 2026-09-08) sudah dibangun defensif: migrasi v12 bybit_positioning + _v5 retry-1 + COALESCE-upsert self-heal + NULL-on-outage. | **RESOLVED (sebagian) 09-10** — teori "flaky" ternyata menutupi 2 fakta permanen (diverifikasi live + DB: 60 baris, ls_ratio & taker 100% NULL selama 2 hari): (a) `/v5/market/account-ratio` MENGUBAH BENTUK RESPONS — field `accountLongRatio` dihapus, kini `buyRatio`/`sellRatio` (share 0..1, semantik sama) → KeyError selama ini; fix = parse buyRatio dengan fallback accountLongRatio (live: ls_ratio landed 0.573/0.568/...); (b) `/v5/market/taker-volume` = **404 permanen, path dihapus Bybit** (kayaknya digabung ke account-ratio) → leg dipensiunkan dari harvest loop, kolom taker_buy_ratio dibiarkan NULL sampai ada endpoint pengganti. Konektivitas TCP-intermiten tetap berlaku untuk endpoint yang hidup. |
| D-021 | **FARSIDE MATI SENYAP 3 minggu (08-21→09-09) + 2 bug terverifikasi live, FIXED 2026-09-10** (audit utilisasi 6 sumber): (a) MODE A — setiap fetch terjadwal gagal & DITELAN: f2 hanya print ⚠ ke stdout tanpa baris fetch_log, dan daemon exit-0 hanya menyimpan BARIS stdout terakhir (untuk f2 selalu baris LME) → kegagalan tak terlihat di fetch_log DAN log daemon; (b) MODE B — halaman Farside menyajikan baris tanggal urutan NAIK (terlama dulu) tapi parser memilih `trs[0]` dengan komentar `# latest` → fetch sukses mengembalikan tanggal basi 19 hari, upsert idempotent menulisnya berulang, MAX(date) tak pernah maju, exit-0 tanpa sensor — tabel membeku permanen bahkan dgn jaringan sempurna. Brief menampilkan flow 08-20 seolah hari ini. FIX: parser max-by-parsed-date (TRAP BARU: *jangan pernah percaya posisi DOM — pilih max dari nilai ter-parse*; saudara F-K6) + skip baris placeholder intraday ('0.0' telanjang / baris parsial < jumlah issuer — baris 12-sel TANPA kolom Total tidak boleh menebak agregat: sel terakhir adalah nilai issuer BTC-mini, bukan Total) + upsert seluruh jendela ~12 baris (gap self-heal: lubang 3 minggu pulih otomatis) + fetch_log FARSIDE:BTC/ETH dgn gerbang stale 3 hari-bursa + daemon menyimpan 3 baris stdout terakhir. **Bug bulan SAFE ikut ketahuan**: sel tonase duplikat di pasangan kolom USD/SDR, pemindaian dari kanan mendarat di sub-kolom SDR tanpa header → fallback now() → datum Agustus tersimpan berlabel 2026-09; fix = baca kolom USD (ganjil) saja; baris salah-label dihapus dari DB (nilai duplikat Agustus). | FIXED (live 09-10: BTC -120.2M$/ETH +34.7M$ @09-09 + 11 baris jendela; gap 08-21→09-09 pulih). Trap tercatat di flows_extra.py docstring + tests/test_flows_extra.py |
| D-022 | **LME off-warrant harian = login-gated (audit 09-10, Notice 25/054 diverifikasi)**: sejak 2025-04 laporan off-warrant HARIAN = T+1 berbayar $1.200/thn (portal lisensi) atau T+3 gratis TAPI butuh login LME.com; arsip bulanan anonim BEKU di 2025-02. | **RESOLVED 09-10 (owner login-path GO awalnya SKIP, lalu aktifkan)**: (a) baseline bulanan anonim 61 bulan landed `lme_offwarrant_cu`; (b) owner daftar akun gratis + export cookie → `LME_COOKIE` env → fetcher harian T+3 `lme_owsr_cu` LANDED live (jalur diverifikasi: `/Lme-api/ReportsListingSearchApi/Get?searchConfigId=…` + `/Download?id={ItemId}`; retensi listing ~7 hari — kelas retensi CME-settlements, histori terakumulasi via harvest harian; cookie mati = fetch_log ERROR bernama "cookie expired — re-export"). **Catatan metodologi**: seri harian 'OWSR Reconciled Inventory' TIDAK dijahit ke arsip bulanan pra-2025-02 (scope berubah per Notice 23/090 — semua logam merek-LME di gudang terdaftar) → dua kind terpisah. Integrasi: baris brief `Cu physical: … · off-warrant Xt (Y% of LME)` (disembunyikan bila >7hr basi) + detail alert copper_stocks_drain. |
| D-023 | **PHASE DECISION 2026-09-13 (owner): BRIEF + TELEGRAM DELIVERY DIPAUSE** — fokus sistem pindah ke pengumpulan & pemrosesan data ("garbage in garbage out"; delivery akan difokuskan ulang belakangan dalam bentuk bot/UI). Implementasi = jadwal daemon `brief`+`send` dikomentari (4 baris; reversible), KODE TETAP ADA: brief menyimpan gerbang identitas harian + golden tests yang adalah permukaan QA data — menghapusnya justru menghilangkan detektor kualitas. Watcher (alert) tetap hidup (konsumen data, bukan brief). Jangan lupa: saat delivery aktif kembali, `docs/explained` + UX-SPEC perlu revisi. | ACTIVE — bukan defect; catatan keputusan arah |
| D-023 | **FASE DATA-FIRST (owner 2026-09-13)**: brief/Telegram DIJEDA sengaja — fokus pengumpulan+validasi data dulu ("garbage in garbage out"). Brief masih digenerate tiap pagi (9 entri), hanya pengirimannya yang pause; watcher tetap jalan. | Lanjut sampai owner akhiri fase | N/A — keputusan owner |
| D-024 | **VERIFIKASI DATA 3-LAPIS 2026-09-13** (12 agen, 87 pemeriksaan): 70% hijau, validity terkuat (verify-gate 114 seri 0 pelanggaran). **4 P1 SEMUA FIXED hari yang sama**: (1) lubang 04-Sep 26 seri → fetch_window gap-heal (nyfed/eodhd/fiscal land seluruh jendela; 67 baris pulih, 114 OK/0 ERROR); (2) COT swap double-underscore NULL 158 minggu → fixed + re-harvest 6.400 baris (gold swap short 253.855 = 61,7% short emas kini hidup); (3) Bybit funding/OI mati senyap → BYBIT:FLOWS fetch_log bernama + gate freshness funding + COALESCE upsert (teruji live: outage tertangkap ERROR bernama); (4) ADS kolon-timestamp → normalisasi + migrasi 6 baris. P2: gerbang umur bulanan copper (watcher skip >45d + brief label umur), VIX 08-Sep backfill, sentinel 08-31 dibersihkan. **DITOLAK sebagai bug**: SOMA agency snapshot-identik (siklus paydown bulanan, endpoint teruji hormati asof); LME 'feed mati' (kanal memang bulanan — file Sep belum ada di server). **KEPUTUSAN FASE**: 12 koleksi TAK-DIBACA tetap ditulis (data-first = kumpulkan dulu, konsumen menyusul); jendela staleness sadar-rilis + fetch_log utk 6 job buta = deferred; Yahoo bad-print 09-10 (GC1/SI1/HG1/PL1) dibiarkan (tanpa konsumen; guard vs settle CME = deferred). | 428/428 test; review adversarial per temuan |

| D-025 | **RONDE-3 (33 agen): BELUM CLEAN — 1 P1 baru (lagi dari fix sendiri) + batch P2; P1 FIXED commit round-3**: c5fa905 melebarkan earnings 21d→42d TANPA paginasi → page-0 kap 4.000 baris (newest-first) memotong minggu TERDEKAT (yang justru actionable) — dibuktikan live: page-1 membawa 1.230 baris hilang; FIX: loop halaman di misc.fetch_earnings_calendar + live-land 5.228 baris (1.219 minggu dekat). P2 FIXED: outbox klaim kini TERBARU-SAJI per kanal (backlog jeda multi-hari tak lagi meledak saat resume — baris lama jadi superseded); print 21d→42d; docstring swap__. **P2 TERSISA (ronde-4)**: (1) WINDOW_FALLBACK tercatat dgn status OK (tak terlihat di rasio health — terjadi live 22:14); (2) copper legacy_code 085691 = seri mati 1989 (yang hidup 085692) — 95 baris sampah, tanpa konsumen; (3) backlog-alerts resume mid-day = no-op senyap s.d. besok (PAUSED_JOBS konstanta per-proses); (4) funding completeness (2-dari-3 fixing), stablecoin freshness, ticker-fallback traceless, EODHD window-shrink, jendela staleness sadar-rilis, Yahoo bad-print 09-10, duplikat events, 6 job fetch_log buta, fedwatch tail non-monotone — semua masih open dgn bukti ronde-3; (5) stamp komentar '2026-09-13' pada commit 09-12 (kelalaian TZ komentarku). | Konvergensi: ronde-4 disarankan |
## C. GATE — soak 7 hari
| D-026 | **KLAIM instrument_prices-duplikat DIVERIFIKASI 13-09 (setengah-nyata)**: 2 baris/hari/simbol NYATA (85.160 hari; PK symbol,ts,source — desain dua-sumber) tapi pipeline ark-watch TIDAK terdampak — keenam pembaca produksi (cot_signals×3, xccy, synthetic, watcher, backtest) memfilter AND source= (deris bersih 1-baris/hari). Gejala setengah-jendela hanya kena ANALISIS EKSTERNAL tanpa filter source (kasus workflow weekly-outlook: BTC below-SMA50 salah → above SMA50/100/200 setelah dedup). Hardening landed: view instrument_prices_clean (1 baris/hari, EODHD-preferred, kolom close_eodhd/close_yahoo/src_divergence_pct = detektor bad-print gratis 0,55% BTC 08-09) + memori dikoreksi vonisnya. | CLOSED — pipeline aman; analisis ad-hoc wajib source= atau pakai view |

| D-027 | **RONDE-4 (27 agen): BELUM CLEAN — 1 P0 struktural baru + insiden operasional**. (A) **P0 events uid-schism**: dua skema event_uid hidup serentak (calendar.py:191 date-uid sha1(name|DATE|US) vs surprise.py:86 ts-uid sha1(name|FULL_TS|US)) — 92,5% baris (9.927/10.728) memakai skema yang TIDAK cocok dgn upsert kalender → aktual tidak pernah terisi utk baris itu (surprise_z kelaparan); MAX(actual),MAX(consensus) surprise jg bisa memasangkan baris silang (JOLTS). PERLU SESI DEDIKASI: unifikasi uid + migrasi re-key + re-harvest actuals. (B) **P1 terkonfirmasi**: konsensus-konflik Empire dsb (baris skema-lama tersisa; z memihak nilai benar +15 — benign tapi baris tua perlu bersih); PPI 4,7-vs-5,4 TERADJUDIKASI benar (5,4 = aktual Agu; 4,7 = previous Juli — DB benar). (C) **Insiden**: daemon mati 03:14 WIB (PC mati malam) + **lockfile PID-recycling** (PID 3392 dipakai ulang svchost → os.kill(pid,0) true → startup ditolak) — lock dihapus manual, daemon restart 20:12, catch-up jalan; pelajaran: cek lock harus verifikasi nama-proses, bukan sekadar PID hidup. (D) **P2 verified-still-open (13 item D-025 + 3 baru)**: WINDOW_FALLBACK status-OK ( + noise 6 seri non-FS by-design — fetch_window harus return None utk unsupported, bukan raise); copper-legacy re-fetch HARIAN (285 baris 1986-89 ditimpa tiap run); PAUSED_JOBS restart-dependent; funding 2-of-3; stablecoin freshness; ticker-fallback traceless; EODHD span; staleness windows; Yahoo bad-prints; events 176 duplikat-grup; 6 job buta; fedwatch tail; ETH funding tak tersimpan; outbox-supersede bisa membunuh row sending segar (<10 mnt); earnings pagination unbounded-page risk; view divergence=1.0 utk eodhd-only (harusnya NULL). | Ronde-5 = sesi dedikasi P0 events + batch P2 |
**VONIS AKHIR 2026-09-10 (trias workflow 22-agen; jendela 09-02 15:36 → 09-09 15:36 WIB).** Dua outage D-019 (PC shutdown) eksogen terhadap software — gerbang yang gagal HANYA karena itu ditandai asterisk.
| D-028 | **RONDE-5 (28 agen) + FIXES (a8865d5, a0af04f)**: inti P0 events-uid fix terverifikasi UTUH (0 mismatch, 0 duplikat, upsert menjangkau semua baris) — tapi ditemukan 3 P1 baru yg SEMUanya fixed hari sama: (1) TIDAK ADA jalur heal utk aktual-NULL >3 hari (jendela pull kalender now-3d; backfill FMP INSERT-OR-IGNORE tak pernah mengisi) → NFP Agu beku NULL; FIX: backfill kini pakai conditional-upsert + heal 60 hari landed (22 aktual; NFP 162k/z+0.94; CPI MOM/YOY kembar FMP terisi; sigma+z recompute); (2) fix P0 tanpa test regresi → test_event_uid.py (3 test pin invariant); (3) rilis penting beku = gejala #1. **P2 batch fixed**: fetch_window unsupported return [] (noise WINDOW_FALLBACK hilang); earnings page-cap 10; copper-legacy gate retired-series; outbox supersede sest fresh-sending (<10mnt); view divergence NULL utk single-source (39% baris sentinel 1.0 dibuang). **P2 tersisa terverifikasi open (10, semua kelas hardening/defer)**: name-twins fragmentasi indicator-family (ESI multi-count); release-time tak ter-update on-conflict; staleness sadar-rilis (30/115 false-flag); 6 job f2 fetch_log-blind; funding completeness 2-of-3; stablecoin freshness; ETH funding tak tersimpan; EODHD span check; Yahoo bad-prints 09-10 (jalur deteksi via view ada); fedwatch tail; PAUSED_JOBS restart-note. 2 temuan DITOLAK. | 431/431 test; konvergensi menuju nol berlanjut |

| D-029 | **RONDE-6 (sweep 10 P2, fe80533) + RONDE-7 (verdict 16 agen, 13 confirmed, 5cee2ab)**: ronde-6 melewati 8 P2 (release-time update, staleness M75→M95/Q190/A550, funding >=2/3, stablecoin gate, funding_eth v15, EODHD span guard, Yahoo quarantine, fedwatch tail) + 2 triage-defer (name-twin ESI mapping, 6 job blind). **Ronde-7 menangkap 2 REGRESI ronde-6** (pola berulang) + honesty gaps: (1) P1 funding gate menolak feed sehat di SEMUA jam run realistis (slice hari-terbaru = 1 fixing pasca-00:00 UTC; ERROR harian + fallback instan tertulis sbg EOD-avg) → FIX slice KEMARIN (hari UTC lengkap terakhir); (2) P1 klaim fedwatch-tail-NULLed PALSU — generator regenerasi tiap run → FIX cap horizon 550hr di writer + re-NULL; (3) P1 brief FedWatch/ECBWatch menampilkan snapshot TERLAMA utk meeting berikutnya (tie meeting_date resolve ke baris scan-order; brief 09-16 tampil probabilitas basi 19 hari di hari FOMC!) → FIX date=MAX(date) tiebreak + pairing meta delta_bp ke meeting yg ditampilkan; (4) P2 CNN F&G renderer age gate; (5) P2 release-time update ter-gate actual-fill → statement sendiri; (6) P2 stablecoin zero-guard; (7) P2 ANXIOUS gate 130d sendiri (Q blanket 190 akan menghidupkan survey 2-siklus-mati); (8) P2 z-vintage drift (314 baris ESI-window tak reprodusibel dari σ tersimpan — audit-trail, bukan angka salah; defer: simpan sigma_vintage); (9) P2 klaim 'divergence detector' utk simbol futures = NULL by design (doc-honesty). | 432/432; ronde-8 diperlukan utk konfirmasi |
| ID | Gate | Vonis | Bukti inti |
|---|---|---|---|
| G-001 | F1 harvest tanpa ERROR permanen | **PASS** | 515 fetch_log = 508 OK/5 ERROR/2 EMPTY; query ERROR ≥3 hari = KOSONG (semua 5 = transient 1-hari: WRESBAL 502, fiscalx 400×1 hari — pulih). Caveat jujur: keluarga gray f2 tak menulis fetch_log di jendela (pra-fix D-021) → "no ERROR" di sana = absensi-bukti; kini terfix |
| G-002 | 7 brief + identitas hijau + Sabtu | **PASS\*** | Semua hari-daemon-hidup menghasilkan brief terkirim (identity 4/4 green; catch-up pasca-restart 8 menit). Asterisk: varian Sabtu 04:15 BELUM PERNAH jalan produksi (daemon mati 2,5 jam sebelum jadwal) — sub-kriteria tetap OPEN s.d. Sabtu pertama daemon hidup |
| G-003 | settlements/cvol/fedwatch ≥5 tgl tanpa gap>3hr | **FAIL → RESCUED** | CVOL 5/5 PASS; settlements asli 2/5 di-jendela. Pasca-window (09-10 14:03-14:40): 09-03+09-08+09-09 di-backfill SEMUA (retensi sempat memuat) → seri kini kontinu kecuali 09-05 permanen + 09-07 libur + ES-strip 09-08. Fedwatch 3/5 = proyeksi langsung dari lubang settlements (kini tertutup) |
| G-004 | 14 hari kalibrasi alert | **FAIL as-written (artefak kalender)** | Tabel alert lahir 09-01 — 14 hari mustahil ada di jendela. Sisi kualitas BERSIH: 64/64 terkirim, 0 gagal, NOL sentinel −10000 di jendela (insiden 08-31 pra-soak). Observasi tuning: kondisi persisten re-fire ~6 jam (chatty, tiap fire kondisi-true). RE-EVAL 09-14 bila daemon hidup |
| G-005 | Vintage hidup pasca-revisi FRED | **PASS** | 28 baris vintage-revisi asli di-jendela (NFCI 09-03 = revisi pertama pasca-soak; PAYEMS/TOTBKCR/TOTLL 09-08) + dual-storage realtime+vintage terverifikasi utk pasangan ts sama |

**SOAK DITUTUP**: software lulus inti (G-001/002/005; G-003 pulih via backfill; G-004 salah-scope). Pembelajaran sistemik: (1) gerbang "≥5 trade-date" tak memodelkan retensi-5-hari + outage-2-hari → ke depan gerbang retensi-sensitif perlu klausa catch-up; (2) observabilitas keluarga gray (fetch_log) = prasyarat gerbang — D-021 membuktikan absensinya membiarkan kegagalan 3 minggu.

## D. RIWAYAT SELESAI


### Gelombang 7 — triase & pengguliran tracker (2026-09-10)

Workflow 22-agen (5 investigator + 17 verifikator adversarial) memeriksa SETIAP item §B terhadap kode+DB+probe live. Vonis + eksekusi hari ini:

| Item | Vonis | Bukti |
|---|---|---|
| D-001 CNN F&G | **CLOSE_STALE** (terkonfirmasi) | CNN live penuh: skor+9 komponen+momentum+250d histori (gelombang-6, commit 93c1392) — "nilai tambah rendah" usang |
| D-011 VIX9D/6M | **CLOSE** formal | Sudah revived 09-08 + live s.d. 09-09 (3.942/4.700 obs); vixterm wired |
| D-012 nber.py | **CLOSE_STALE** (terkonfirmasi) | Dipakai qa/f4.py:248 (replay) + __main__.py:73 — "unused" usang |
| D-016 TZ per-sumber | **CLOSE** — 7 sumber terverifikasi live 09-10 | TV: explicit-Z UTC (Claims/PPI 12:30Z = 08:30 EDT ✓); EODHD: naive=UTC benar (CPI 12:30 ✓); FRED: kontrak tanggal-murni (10-char, tanpa TZ); CME kalender: 12:30Z ✓; Bybit: epoch-ms→UTC di 3 jalur ✓; NYFed: effectiveDate tanggal-murni ✓; FMP: 12:30 UTC ✓. Tidak ada bug TZ |
| D-017 CBOE P/C | **CLOSE** (terkonfirmasi) | cnn_put_call_options 249 hari (2025-09-10→2026-09-09) = bacaan kontrarian pengganti; kondisi re-pin CBOE tetap dicatat |
| D-013 misc.py | dipersempit (bukan stale penuh) | RECESSION_PROB live via misc; tersisa holidays+MPT |
| D-006 ECB | dipersempit | ESR path live; tersisa ESTRWatch |
| D-004 episode | PARTIAL (bukan unbuilt) | mesin ada di f4.py; tersisa kalibrasi+pin |
| D-018c heartbeat | **FIXED** (commit hari ini) | _heartbeat() dalam _run_job |
| D-014 | minggu 09-02 di-RESCUE manual | lihat anotasi D-014 |

Plus: backfill CME 09-03/09-08 (retensi race dimenangkan), vonis gerbang §C ditulis.

### Gelombang 6 — ekspansi utilisasi 6 sumber flow (2026-09-10)

Audit 6 sumber (Farside/SAFE/LBMA/TIC/CNN/LME, 5 agen workflow) → owner GO penuh (1-6 + per-issuer + off-warrant). Semua live-verified dalam satu run f2:

| Sumber | Sebelum | Sesudah |
|---|---|---|
| Farside | BROKEN (D-021): 1 angka agregat basi 3 minggu | Jendela 12 hari + per-issuer 12 BTC/10 ETH issuer (tabel v13 etf_flows_issuer) + kumulatif sejak-inception + gerbang stale |
| CNN F&G | 1 skor, filed di tanggal RUN (dead-key ts) | Skor @ tanggal laporan + 9 komponen skor + momentum prev_close/1w/1m/1y + **backfill 249 hari** komposit + P/C + VIX mentah (pengganti CBOE P/C D-017) |
| LBMA | silver saja, 2 bulan | gold + silver, **122 bulan** (2016-07→) satu file; label brief LBMA Au/Ag; fallback URL /downloads/ |
| SAFE | 1 angka tonase, bulan salah (D-021b) | 8 bulan histori (beli-berturut PBoC terlihat: 7.419→7.673万oz) + gold share 9,1% + nilai USD/FX/total |
| TIC | China saja, label provenance salah (mfh.txt) | 7 baris (China/Belgium/Japan/Cayman/UK/Grand Total/Official Bills), host kanonik ticdata.treasury.gov; brief: komposit CN+BE stealth + foreign total |
| LME | stok harian saja | + arsip off-warrant 61 bulan baseline (D-022) + catatan release_schedule jujur |

**Koreksi klaim audit** (diverifikasi live): (a) TIC table5 hanya menampilkan 1 kolom angka terisi — "backfill 13 bulan dari HTML sama" TIDAK mungkin (13 periode = dropdown); (b) CNN histori 250 hari & LBMA 122 bulan sudah ada DI DALAM payload/file harian — backfill gratis tanpa arsip terpisah; (c) gerbang TIC 45 hari terlalu ketat (rilis M+2 → dikalibrasi 80 hari; rilis Juni masih resmi terbaru per 09-10).

Suite 362 → **387** test (25 baru: parser Farside/SAFE/LBMA/CNN/off-warrant + landing self-heal + stale-gate + brief). Brief baru: `ETF flows (09-09): BTC -120M$ (GBTC -27·IBIT -20) · ETH +35M$ (...)` + `PBoC Au 2,387t (26-08) · Au share 9.1% · LBMA Au 9,633t · LBMA Ag 28,431t` + `TIC CN 633B$+BE 483B$=1,116B$ · foreign total 9,299B$ (26-06)` + `Fear&Greed: 39 (Fear) · 1m-ago 64` + baris split `F&G split:` saat divergensi komponen ≥40.

**Lanjutan gelombang-6 (sore 09-10):** (a) owner mengaktifkan jalur login LME → harian T+3 `lme_owsr_cu` landed (4 file, CU 114→125k t; brief baris Cu physical + detail alert copper_drain; commit d1173ce) — D-022 RESOLVED; (b) audit "apakah ISSUE terbaru" membongkar 2 WATCH: D-019 akar masalah = PC shutdown/restart (Windows Event Log; daemon restart 13:47 + catch-up) dan D-020 = Bybit reshape account-ratio + taker-volume 404 (fix + leg dipensiunkan); suite 387 → **395**.

### Gelombang 5 — tiga integrasi data penuh + verifikasi konvergen (2026-09-03..04)

Ekspansi utilisasi sumber per mandat owner ("FED 100% → CME → NY Fed"), semuanya: riset live-probe → PLAN doc (dihapus pasca-eksekusi per pola owner, riwayat di git) → build 2 agen paralel (fetch/sinyal, DDL terkunci) → verifikasi main-session → **reviewer adversarial multi-lensa → fix → ronde konvergen**. Suite 84 → **245/245**; ruff bersih di setiap commit.

| Build | Commit | Isi | Verifikasi |
|---|---|---|---|
| SOMA per-CUSIP (v7) | 660e637 → 82d2ae1 → 418b1c6 | 2 tabel, 108 mgg backfill; endpoint asli `/tsy/get/asof` (path plan 400); **identitas ΣchangeFromPriorWeek vs Δtotal_par 107/107 mgg ke dollar** (gross ≠ portfolio change — par jatuh tempo keluar snapshot diam-diam); net-liq FR-34 penuh; repo-specials isu-baru-cap-70%; tripwire WALCL-gap | ronde-1: 3 bug (daemon cmd, cooldown non-permanen, jendela entrant gap-6 hari) ad630fe · ronde-2: 2 P1 + 8 P2 (alert lost-forever → outbox terminal-failed; ΔWALCL fiktif-0 → guard baris-sama; restatement drift → replace atomik; time-bomb fixture tanggal) ec21c41 · ronde-3 konvergen eb83429 |
| CME options per-strike + CVOL yield-vol (v8) | b810305 | 23.681 baris opsi 7 produk (kontrak API mahal-13-probe: opsi = produk ber-id sendiri, monthYear=contractId, `/OOF`); PCR/walls/max-pain; **bonus: bug pra-eksisten tradeDate non-ISO di fetch_settlements ketahani & difix** (+393 duplikat dibersihkan) | live-verify penuh; trigger PCR diam-by-construction s.d. 20 hari histori |
| NY Fed Markets full (v9) | bb0dea2 → 857b0a8 → daf95e4 | 9/10 keluarga (agency MBS **$1,906.17B = summary resmi sampai dollar**, desk ops, PD survey 8×112 obs, 12 seri rates, fxs tripwire); Net Liq v2 split MBS/other; blok DLR + 3 trigger | ronde-1: 0 P0/12 P2 (ambs-tak-menjelaskan-tsy, tanda pct base-negatif, span-holiday, gerbang kedalaman 44d, stale-marker, isolasi fxs, fp stabil) 857b0a8 · ronde-2 KONVERGEN daf95e4 |

Latent L1 (bawah) **terpecahkan di gelombang ini**: outbox kini menandai status='failed'+last_error di cap attempts (ec21c41) — baris tak lagi menggantung 'pending' selamanya.

Dokumentasi: restrukturisasi docs owner di-final-kan 296a799 (lihat D-015); PLAN-SOMA/PLAN-CME-OPTIONS/PLAN-NYFED-FULL dihapus pasca-eksekusi (retrievable: `git show <commit>^:docs/<nama>.md`).

### Gelombang 4 — verifikasi final ronde-3 (2026-09-02): **KONVERGEN — 0 temuan baru**

Seluruh 8 area yang diubah ronde-2 diverifikasi baris-demi-baris + uji empiris (simulasi split 4-skenario, DB in-memory semantik `IS NOT`, kalkulasi Senin-ke-3 independen 120 bulan = 0 mismatch, jalur migrasi fresh v1→v6). Semua BENAR. **Workflow iteratif dinyatakan selesai: ronde-1 → ronde-2 (regresi ditemukan & diperbaiki) → ronde-3 (bersih).**

Catatan laten non-blokir (dicatat, tak dieksekusi):
- L1: ~~alert gagal-kirim >25 menit outage → terminal attempts=5 dengan status 'pending'~~ **TERPECAHKAN gelombang-5** (ec21c41: outbox menandai status='failed' + last_error di cap; 'failed' dikecualikan cooldown → kondisi bisa re-fire pasca sender pulih)
- L2: mesin split sadar-`<pre>` dorman (brief nyata tak ber-tag pre); bila kelak pakai `<pre>`, escape per-chunk akan menetralkan tag → butuh escape sadar-tag saat itu

### Gelombang 3 — rescan ronde-2 (2 agen verifikasi, 2026-09-02)

**2 KRITIS diintroduksi fix ronde-1 (bukti empiris agen) + 8 SEDANG/LOW — semua FIXED + regresi test:**

| ID | Temuan | Fix |
|---|---|---|
| R2-K1 | `_split_message` IndexError saat boundary di luar `<pre>` (current kosong) + escape-sebelum-split membuat pre-aware dead → **semua brief >4000 char crash job send** (test 60/60 tak menyentuh fungsi ini) | guard list-kosong + split-mentah-escape-per-chunk + test_round2 (6 kasus) |
| R2-K2 | Brief Sabtu 04:15 WIB disimpan kunci **UTC = Jumat** → menimpa brief Jumat + outbox Sabtu tak pernah dibuat (IGNORE kena baris Jumat-sent) + guard tak berfungsi — fitur Sabtu ronde-1 mati total | kunci brief_log/brief_deliveries = tanggal **WIB** |
| R2-S1 | Daemon key `cmd@date` → f2-Sabtu-03:45 MEMBLOKIR f2-harian-08:30 (flows Sabtu tak refresh; gagal 03:45 = tanpa fallback seharian) | key per-entri jadwal `hhmm-cmd@date` (dibuat _due_jobs, identik run_loop) |
| R2-S3 | `_imm_approx` mengembalikan Senin ke-**2** (range 7-21) bukan ke-3 → τ salah 7 hari, basis ~2-3% terlalu besar (SEP 26 = 14 Sep, harusnya 21) | range(14,21) + test 15≤d≤21 |
| R2-S4 | Tampilan funding `> -1` (sentinel era-lama) memblokir funding negatif sah −3.5bps | `is not None` |
| R2-S5 | Anchor EFFR query tanpa `vintage_ts='realtime'` — baris vintage revisi (ts sama) bisa terpilih; **4bp anchor ≈ 16pp probabilitas** | filter vintage |
| R2-S6 | Consensus beku nilai-pertama (listing 14-hari pra-rilis dari sumber rendah); spec §3-I: konsensus boleh upsert pra-rilis | statement UPDATE terpisah (syarat actual IS NULL) — upsert multi-syarat tak didukung SQLite |
| R2-S7 | `fetch_cme` masih ekspansi-string K/M ("1.5M"→1.5) — kelas bug yang sama baru saja difix di `_num` | pakai `_num` langsung |
| R2-S8 | Marker GLD approx baca baris arbitrer riwayat harian (tanpa MAX period) → marker bisa terbalik | MAX(period) |
| R2-S9 | TIC `periods` dari seluruh HTML (tanggal rilis non-tabel bisa jadi label) → pasangan (ts,nilai) tak dijamin satu kolom | run ≥3 sel periode dari header tabel (sel kosong diabaikan) — live: 2026-06/633.4 ✓ |
| R2-S10 | Dead-guard cut Sabtu (`_idx_of("Sumber:")` dihitung sebelum baris di-append) | potong eksplisit sampai akhir |
| R2-S3e | Retry alert hanya lewat job send 07:05 harian (latensi s.d. 24 jam utk alert trading; docstring watch menjanjikan tiap siklus) | watch-cycle retry + pacing `last_attempt` 5-mnt (migrasi v6) — **terbukti live: ↻1 terkirim** |

**Diverifikasi BENAR oleh rescan (tidak diubah):** fedwatch running-rate (repro angka + test koheren); harvest upsert+vintage idempoten; claim atomik brief; watcher funding/kurva/pasangan-mm-lev; xccy INTERSECT+kuartalan; TIC parser nilai; daemon retry/HARI/lockfile (os.kill(pid,0) diverifikasi aman di Windows); surprise MAX(as_of); gate FedWatch match; units/backup/alfred/cot/spdr.
**Catatan jujur:** e2e-note — settlement CME corrected tak pernah masuk (OR IGNORE konsisten append-only, dicatat); revisi FRED 2×-sehari: nilai antara hilang dari vintage (hanya 1 slot/hari, LOW).

### Gelombang 2 — workflow scan iteratif ronde-1 (5 agen, 2026-09-02, commit f28263d)

| ID | Temuan | Fix |
|---|---|---|
| F-K1 | Smart-money BTC ΔLONG dilabel ΔSHORT (arah terbalik) | indeks [4] |
| F-K2 | Quadrant OI kontrak arbitrer + kategori-sama-tanggal → ΔOI≈0 fiktif | mapping kontrak + GROUP BY minggu |
| F-K3 | z-window 1260 vs obs 120/300 → pilar C/D/E z=None selamanya | window per-frekuensi |
| F-K4 | Event pra-rilis beku tanpa actual → ESI kelaparan | ON CONFLICT fill-if-NULL |
| F-K5 | Brief FedWatch tampil meeting lampau | filter >= hari-ini |
| F-K6 | Tanda negatif Farside hilang utk "(1,234.5)" | deteksi kurung di match mentah |
| F-K7 | XCCY sort alfabetis → kontrak terjauh jadi headline | sort by IMM |
| F-K8 | WALCL "6743.00T" (1000×) | scale per-series |
| F-S1..S14 | dedup σ rowid, ESI dobel-bobot, watcher campur _c, funding negatif diblok, xccy kaki campur-tanggal + 1/τ blowup (−414bp) + bulanan noise → kuartalan verified-scope, EFFR hardcode, net-liq label, quadrant INSUFFICIENT, flows_daily REPLACE-wipe | semua (lihat commit) |
| F-G | GDPNOW first-print beku (FRED revisi in-place) | upsert + re-anchor kuartal-selesai |
| F-001..005 | alert hilang bila gagal kirim; flows_extra orphan; params_block_c orphan; CVOL dead-write; computed_signals tak pernah ditulis | migrasi v3 + wiring + store_cot_signals |

### Gelombang 2 — workflow scan iteratif ronde-1 (5 agen, 2026-09-02, commit workflow-r1)

**KRITIS baru (semua terverifikasi manual + live):**

| ID | Temuan | Fix |
|---|---|---|
| R1-K1 | **TIC 2 lapis**: (a) kode ambil kolom TERTUA; (b) sumber `mfh.txt` = file BEKU era-2023 (kolom terbaru Jan-2023, China 1033.8 = Feb-2022; benar 633.4 = Jun-2026) | sumber → `slt_table5.html` (live 2026-06) + parse kolom-kiri + ts periode |
| R1-K2 | FedWatch `pre_rate` = rata-rata bulan-sblm (bukan rate akhir bulan) → probabilitas meeting ke-2 TERBALIK mulai Okt-26 ("cut 100%" utk meeting yang dihargai hold) | algoritma running-rate + regresi test skenario angka |
| R1-K3 | z-score pilar C/E dari LEVEL deret monoton → terpaji konstan (+1.7/−1.5) = bias permanen ±0.2-0.26 regime score (87% ambang RISK-ON) | z dari LAJU: YoY (C), Δ4w (E) |
| R1-K4 | Sentinel funding −1(fraksi)×10000 = **−10000bps** → false alert 'SHORT CROWDED' TERKIRIM 31/08 (bukti alert_deliveries) | gagal-fetch = NULL + funding-EOD weighted 3-titik (FR-37) + filter tanggal |
| R1-K5 | Kurva HG sort alfabetis ('APR 27' vs 'APR 28') → label contango/backwardation fiktif di alert copper | sort by tanggal-IMM |
| R1-K6 | Sender claim-race + re-claim tanpa jeda/cap (bukti: attempts=6 > cap 3) | claim atomik BEGIN IMMEDIATE + claimed_at (migrasi v5) + re-claim >10mnt + cap |

**SEDANG/QUAL utama (semua fixed):** CME K/M suffix korup desimal ("1.356M"→1.356) → multiplier numerik; CME 403 dibaca hari-libur → raise; tradeDate dari respons; compare gate match beda-tahun → 'Mon YY' + gate DIWIRE ke f2; GLD shares hardcode 291M (flow struktural mati; live FMP = 353M → 1006t vs 830t) → shares live + marker approx; `disagg_code` key mismatch (legacy copper tak pernah terfetch) + legacy nonrep hilang → fix + fetch legacy; LME loop harian tanpa session bersama; cot.py or-chain 0→None; calendar uid jam-tidak-stabil (duplikat RCM TIPP live) → uid per-tanggal; provenance per-field; alfred infinite-loop + days_back mati → repurpose maintenance FR-54; backup `or True` hapus bulanan → retensi nyata; harvest BEGIN tanpa ROLLBACK (kaskade ERROR) → try/rollback; xccy tahun-4-digit phantom + IMM tgl-15 → Senin-ke-3 eksak + validasi; config YAML-kosong crash → ValueError; explore kolom target + freshness 2026-08-28 hardcode + 0.0=N/A; daemon handler-menumpuk + retry-blokir-heartbeat + nama-hari + tanpa-lockfile → semua; __main__ unknown-cmd exit-0 → exit 2 + path portabel + `export --csv` (FR-58, I-020); Sahm ⚠ disembunyikan filter ✗; flows_periodic render seluruh riwayat → MAX(period); surprise LIMIT-sebelum-filter; hedging 'FALLING' dari nol-info → N/A; n_contracts hardcode 7 → penyumbang nyata; identity slope hard-✓ → status nyata + dead-fetch dihapus; DFII threshold 0.001 (50× terlalu sensitif) → 0.05 berlabel; ICSA "203000K" → scale 1e-3; delta tak terskala → delta×scale; W/M tampil sbg 'semalam' → recency per-frekuensi; header 'data per' klaim palsu → dari obs terbaru; Sabtu tanpa varian COT-pasca-rilis → job sab 03:45 f2 + 04:15 brief + anti-timpa + gating S-2/S-5/S-6; tautologi test (3 kelas tak menyentuh produksi) → test produksi DB-seed; units.py terpusat + test publikasi (menangkap KG_PER_KOZ 1000× saat e2e!); percentile min-obs; z-VIX cooldown sesuai docstring; watcher mm/lev pasangan inert; slack docstring/klaim ±0.00bp jujur; GDPNOW→semua-FRED upsert (I-002) + release_ts forward (I-001 partial).

## E. Metodologi workflow iteratif

1. **Scan** — 5 agen paralel per-slice (signals / transforms+db+config / fetchers / qa+sender / quality), instruksi eksplisit jangan duplikat temuan lama.
2. **Verifikasi manual** — tiap temuan diverifikasi baca-kode/live SEBELUM fix (ronde-1: 6/6 KRITIS terbukti, termasuk 2 temuan berlapis TIC).
3. **Fix + test regresi** — suite wajib hijau (kini 245/245); tes tautologi diganti tes produksi.
4. **Verifikasi end-to-end** — pipeline segar (f2/brief) dijalankan; e2e menangkap bug yang lolos test (KG_PER_KOZ).
5. **Rescan ronde-2** — ulang sampai 0 temuan-valid baru.
6. **Update dokumen ini** — status + commit.
